<tr>
  <td><%= product.title %></td>
  <td><%= truncate_text(200, product.body) %></td>
  <td><%= product.price %></td>
  <td><%= image_tag product_photo(product, :xsmall), class: "img-rounded" %></td>
  <% if user_authorized(@user.id) %>
    <td><%= link_to 'Show', product_path(product) %></td>
    <td><%= link_to 'Edit', edit_product_path(product) %></td>
    <td><%= link_to 'Destroy', product_path(product), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  <% end %>
</tr>

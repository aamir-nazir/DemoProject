<%= form_for([@product, @review], remote: true)  do |f| %>
  <div class="field">
    <%= f.text_area :body, required: true %>
  </div>
  <div class="actions">
    <%= f.submit "Add Review" %>
  </div>
<% end %>